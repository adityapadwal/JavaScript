{
    // this is a block
    // block is also called as a compound statement
    // let and const are block scoped
}  

// Q. What is block?
// => a block is used to combine multiple js statements into a single group

// Q. Why do we use a block?
// => We use a block where js expects at least one statement. 
// This is because js combines multiple js statements into a single group